# Variabili d'ambiente — copia in .env e compila quando servono.
# Nell'MVP del solo motore prezzi NON serve nulla di tutto questo.

# Tenant di default per i test in locale (un solo tenant nell'MVP)
DEFAULT_TENANT_ID=pizzeria-demo

# --- Fasi successive (telefonia + voce), NON ancora usate ---
# GOOGLE_CLOUD_PROJECT=
# GOOGLE_GENAI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash-native-audio
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=
