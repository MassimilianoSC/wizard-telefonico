# Wizard vocale telefonico — dipendenze

# Test
pytest>=8.0

# Backend / API (Fase 2)
fastapi>=0.137
uvicorn>=0.49
websockets>=16.0
python-dotenv>=1.2
python-multipart>=0.0.9   # form-encoded del webhook Twilio (/twiml)

# Telefonia + voce (Fase 2)
twilio>=9.10
google-genai>=2.9

# Audio: mu-law <-> PCM16 e resampling (audioop rimosso dalla stdlib in Py3.13)
audioop-lts; python_version >= "3.13"
